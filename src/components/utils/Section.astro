---
const {id, title, class: className} = Astro.props;
---
<section id={id} class=`flex flex-col mt-20 pt-[10vh] gap-10 mx-auto w-5/6 lg:w-1/2 ${className}`>
    <h2 class="text-center font-semibold text-3xl text-indigo-400 lg:text-left">
      {title}
    </h2>
    <slot/>
</section>